# Legacy entry points — do not use

These files are **not** used by the active Expo app (`frontend/index.js` → `frontend/App.js`):

- `/App.js` (repo root)
- `/index.js` (repo root)
- `/myapp/`

Use `frontend/` for all mobile development and builds.
