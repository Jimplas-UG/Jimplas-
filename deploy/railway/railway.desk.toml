# Railway: Node desk-api (public HTTPS API)
[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/railway/Dockerfile.desk"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
