/** Trader-facing constants only — no strategy thresholds. */
export const SIM_DESK_EQUITY = 50000;
export const DISPLAY_TICK_SIZE = 0.1;
/** @deprecated alias — same numeric edge as strategy pipSize */
export const DISPLAY_PIP_SIZE = DISPLAY_TICK_SIZE;
