# Deprecated — MT5 execution layer

BSV3.2 is **Binance Futures only**. This folder is kept for historical reference (MQL5 parity audits) and is not wired into the app or desk-api.

Use `binance_trading_system/python` instead (`npm run binance-api` from `backend/`).
